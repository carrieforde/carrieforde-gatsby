export interface Frontmatter {
  title: string;
  date: string;
  updated?: string;
  category?: string;
  description?: string;
  showToc?: boolean;
  company?: string;
  location?: string;
  range?: string;
}
